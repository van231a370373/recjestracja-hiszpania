/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tus opciones de configuración de Next.js van aquí si las tienes.
  // Por ahora, un objeto vacío es perfectamente válido.
};

export default nextConfig;

